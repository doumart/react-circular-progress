const { ModuleKind } = require("typescript")
const CircularProgress = require("./lib/CircularProgress")
const GradientCircularProgress = require("./lib/GradientCircularProgress")

module.exports = {
  CircularProgress,
  GradientCircularProgress,
}
